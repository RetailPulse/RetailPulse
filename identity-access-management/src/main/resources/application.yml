spring:
  datasource:
    url: jdbc:mysql://localhost:3306/identity_access
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
logging:
  level:
    org:
      springframework:
        security: TRACE
        security.oauth2:
            server:
              authorization: TRACE
